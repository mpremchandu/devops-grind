# Setup

- OS: Ubuntu (VM)
- User: non-root user with sudo access
- Network: Local LAN (192.168.1.0/24)
- Firewall: ufw
- Tool used for service: Python HTTP server

## Initial Command Used
python3 -m http.server 8080
